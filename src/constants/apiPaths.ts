const API_BASE = 'https://q009ti3ju5.execute-api.eu-central-1.amazonaws.com/prod';

const API_PATHS = {
  product: `${API_BASE}/products`,
  order: `${API_BASE}/`,
  import: `${API_BASE}/`,
  bff: `${API_BASE}/products`,
  cart: `${API_BASE}/`,
};

export default API_PATHS;
